<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
       xmlns:app="http://schemas.android.com/apk/res-auto"
       xmlns:tools="http://schemas.android.com/tools"
       android:layout_width="wrap_content"
       android:layout_height="wrap_content"
       tools:background="@drawable/background_white"
       tools:gravity="center"
       tools:paddingEnd="@dimen/spacing_small"
       tools:paddingStart="@dimen/spacing_small"
       tools:parentTag="android.widget.LinearLayout">

    <my.TextView
            android:id="@+id/ratingTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/spacing_normal"
            android:text="@string/dummy_string"
            android:textColor="@color/red"
            tools:text="1"/>

    <my.ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/background_red"
            android:tint="@color/file3.xml"
            app:cornerRadius="@dimen/spacing_large"/>
</merge>

